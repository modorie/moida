import styled from "styled-components";

import { Theme } from "@/foundations";

export const Layout = styled.div`
  display: flex;
  padding: 0 26px;
  box-shadow: 0 -1px 0 0 ${Theme.borderColor.lighter} inset;
`;
