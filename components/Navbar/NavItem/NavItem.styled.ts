import styled from "styled-components";

export const Layout = styled.li`
  display: flex;
  justify-content: space-between;
  align-items: center;

  height: 50px;
  list-style: none;
  cursor: pointer;

  svg {
    position: relative;
    right: -5px;
  }
`;
